name: 'Build'

on:
  push:
    branches:
      - build-artifacts

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    - run: |
        cmake .
        cmake --build . --target ALL_BUILD --config Release
